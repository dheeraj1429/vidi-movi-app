const INITL_STATE = {};

const userReducer = function (state = INITL_STATE, action) {
    switch (action.type) {
        default:
            return {
                ...state,
            };
    }
};

export default userReducer;
