import styled from "styled-components";

export const div = styled.div`
    display: flex;
    align-items: center;
    justify-content: space-between;
    h4 {
        font-size: 50px;
        font-weight: 400;
        color: var(--main-cl);
    }
    span {
        font-size: 13px;
        color: var(--main-cl);
    }
    p {
        font-size: 14px;
        color: var(--main-cl);
    }
    .extra-sm {
        font-size: 15px;
    }
`;
