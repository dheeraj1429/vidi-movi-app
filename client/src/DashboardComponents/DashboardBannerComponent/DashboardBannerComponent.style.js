import styled from "styled-components";

export const div = styled.div``;

export const innerDiv = styled.div`
    border-radius: 8px;
    overflow: hidden;
    h1 {
        color: var(--main-cl);
    }
    p {
        color: var(--main-cl);
    }
`;
